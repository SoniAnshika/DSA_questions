class Solution {
public:
    int percentageLetter(string s, char c) {
        int l=s.length();
        int c1=0;
        for(int i=0;i<l;i++)
        {
            if(s[i]==c)
                c1++;
        }

        return (c1*100)/l;
    }
};
